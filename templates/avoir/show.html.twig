{% extends 'base.html.twig' %}

{% block title %}Avoir{% endblock %}

{% block body %}
    <h1>Avoir</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ avoir.id }}</td>
            </tr>
            <tr>
                <th>Id_artcile</th>
                <td>{{ avoir.idArtcile }}</td>
            </tr>
            <tr>
                <th>Id_mot_cle</th>
                <td>{{ avoir.idMotCle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_avoir_index') }}">back to list</a>

    <a href="{{ path('app_avoir_edit', {'id': avoir.id}) }}">edit</a>

    {{ include('avoir/_delete_form.html.twig') }}
{% endblock %}
