{% extends 'base.html.twig' %}

{% block title %}Avoir index{% endblock %}

{% block body %}
    <h1>Avoir index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Id_artcile</th>
                <th>Id_mot_cle</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for avoir in avoirs %}
            <tr>
                <td>{{ avoir.id }}</td>
                <td>{{ avoir.idArtcile }}</td>
                <td>{{ avoir.idMotCle }}</td>
                <td>
                    <a href="{{ path('app_avoir_show', {'id': avoir.id}) }}">show</a>
                    <a href="{{ path('app_avoir_edit', {'id': avoir.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_avoir_new') }}">Create new</a>
{% endblock %}
