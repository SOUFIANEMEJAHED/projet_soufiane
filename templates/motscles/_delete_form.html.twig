<form method="post" action="{{ path('app_motscles_delete', {'id': motscle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ motscle.id) }}">
    <button class="btn">Delete</button>
</form>
