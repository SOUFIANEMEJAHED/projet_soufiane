<form method="post" action="{{ path('app_appartien_delete', {'id': appartient.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appartient.id) }}">
    <button class="btn">Delete</button>
</form>
